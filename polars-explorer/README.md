This project is scaffolded using `pnpm create tauri-app` with React and TypeScript.

It uses shadcn-ui for the user GUI.

To run this app in dev mode, first run `pnpm install` to install dependencies.

Then, run `pnpm tauri dev`